import { IndicatorFormatterPipe } from './indicator-formatter.pipe';

describe('IndicatorFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndicatorFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
