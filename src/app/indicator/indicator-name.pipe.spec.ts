import { IndicatorNamePipe } from './indicator-name.pipe';

describe('IndicatorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new IndicatorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
